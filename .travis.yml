language: ruby
cache: bundler
rvm: [ 2.1.2]
# services: [ redis ]
branches:
  only: [master]
before_script:
- psql -c 'create database hn_challenge_test' -U postgres
- "bundle exec rake db:schema:load RAILS_ENV=test"
script:
- bundle exec rspec
bundler_args: "--without development production"
